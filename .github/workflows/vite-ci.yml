name: Vite CI React Build

on:
  - branches: [main]
  - pull-request: [main]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: 📢 Echo
        run: 'echo "My name is ashik"'
      
      - name: 🏃 Checkout into runner
        uses: actions/checkout@v4
      
      - name: ⚙️ Setup nodejs
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      
      - name: 🔬 Dependencies
        run: npm ci

      - name: 🤖 Build react project
        run: npm run build